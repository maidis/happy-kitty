diff -Naur rezerwar-0.4.2.orig/configure rezerwar-0.4.2/configure
--- rezerwar-0.4.2.orig/configure	2010-05-09 19:25:03.000000000 +0300
+++ rezerwar-0.4.2/configure	2010-06-13 16:48:45.787813481 +0300
@@ -216,9 +216,9 @@
 	check_sdl
 	check_sdl_mixer
 	[ -z "$PREFIX" ]	&& PREFIX="/usr"
-	[ -z "$TARGET_BIN" ]	&& TARGET_BIN="$PREFIX/games"
+	[ -z "$TARGET_BIN" ]	&& TARGET_BIN="$PREFIX/bin"
 	[ -z "$TARGET_DOC" ]	&& TARGET_DOC="$PREFIX/share/doc/rezerwar"
-	[ -z "$TARGET_DATA" ]	&& TARGET_DATA="$PREFIX/share/games/rezerwar"
+	[ -z "$TARGET_DATA" ]	&& TARGET_DATA="$PREFIX/share/rezerwar"
 	CFG_DATAPATH="$TARGET_DATA"
 	CFG_CFGPATH=".rezerwar"
 	;;
diff -Naur rezerwar-0.4.2.orig/Makefile rezerwar-0.4.2/Makefile
--- rezerwar-0.4.2.orig/Makefile	1970-01-01 02:00:00.000000000 +0200
+++ rezerwar-0.4.2/Makefile	2010-06-13 17:05:06.194938508 +0300
@@ -0,0 +1,66 @@
+# Automagically generated by 'configure'
+
+TARGET_BIN=${DESTDIR}/usr/bin
+TARGET_DOC=${DESTDIR}/usr/share/doc/rezerwar
+TARGET_DATA=${DESTDIR}/usr/share/rezerwar
+NSIS?=/E/NSIS/makensis.exe
+INSTALL?=/usr/bin/install
+
+all: build
+
+build: ${EXTRA_BUILD}
+	${MAKE} -C src
+
+build_win32:
+	${MAKE} -C data/gfx/icons/
+	${MAKE} -C src win32
+
+install.exe: data/win32/install.nsi
+	cp -f data/win32/install.nsi .
+	${NSIS} install.nsi
+
+clean:
+	${MAKE} -C src/ clean
+
+distclean: clean
+	${MAKE} -C src/ clean
+
+dist:
+	rm -rf .hg*
+	rm -f data/Makefile
+	rm -f data/gfx/Makefile
+	rm -rf data/wii/
+	rm -rf data/win32/
+	rm -f data/gfx/*.xcf
+	rm -f data/gfx/*.svg
+	rm -rf data/gfx/tools/
+	rm -rf data/gfx/icons/
+	rm -f data/music/*.it
+	rm -f data/music/Makefile
+	rm -f doc/DOWNLOADS
+	rm -rf mkfiles/
+
+install:
+	${INSTALL} -d ${TARGET_BIN} ${TARGET_DOC} ${TARGET_DATA}
+	${INSTALL} -d ${TARGET_DATA}/gfx
+	${INSTALL} -d ${TARGET_DATA}/music
+	${INSTALL} -d ${TARGET_DATA}/sfx
+	${INSTALL} -d ${TARGET_DATA}/levels
+	${INSTALL} -d ${TARGET_DOC}
+	${INSTALL} -m 644 doc/* ${TARGET_DOC}/
+	${INSTALL} -m 644 data/gfx/*.bmp ${TARGET_DATA}/gfx/
+	${INSTALL} -m 644 data/levels/* ${TARGET_DATA}/levels/
+	${INSTALL} -m 644 data/sfx/*.wav ${TARGET_DATA}/sfx/
+	${INSTALL} -m 644 data/music/*.ogg ${TARGET_DATA}/music/
+	${INSTALL} -m 755 src/rezerwar ${TARGET_BIN}/
+
+uninstall:
+	rm -f ${TARGET_BIN}/rezerwar
+	rm -f ${TARGET_DATA}/gfx/*.bmp
+	rm -f ${TARGET_DATA}/music/*.ogg
+	rm -f ${TARGET_DATA}/sfx/*.wav
+	rm -f ${TARGET_DATA}/levels/*.lvl
+	rm -f ${TARGET_DOC}/*
+	rmdir --ignore-fail-on-non-empty ${TARGET_BIN} ${TARGET_DATA}/gfx \
+		${TARGET_DATA}/music ${TARGET_DATA}/sfx ${TARGET_DATA}/levels \
+		${TARGET_DATA} ${TARGET_DOC}
diff -Naur rezerwar-0.4.2.orig/src/config.h rezerwar-0.4.2/src/config.h
--- rezerwar-0.4.2.orig/src/config.h	2010-05-09 19:25:37.000000000 +0300
+++ rezerwar-0.4.2/src/config.h	2010-06-13 17:05:06.186938250 +0300
@@ -1,6 +1,6 @@
 /* Automagically generated by 'configure' */
 
 #define VERSION "rezerwar 0.4.2"
-#define DATAPATH "/usr/share/games/rezerwar"
+#define DATAPATH "/usr/share/rezerwar"
 #define CFGPATH ".rezerwar"
 #define WITH_SOUND
